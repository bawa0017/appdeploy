apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-db
  namespace: test01
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: gp3   # or gp2
